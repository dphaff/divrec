# UK Brokerage Dividend Crediting & Reconciliation Engine (GBP, v1)

Python-only CLI + tests (pytest). `src/` layout. No pandas.

## Setup (venv)

```bash
python -m venv .venv
# macOS/Linux:
source .venv/bin/activate
# Windows (PowerShell):
# .\.venv\Scripts\Activate.ps1